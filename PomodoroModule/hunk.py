from pomodoro import Pomodoro

class Hunk:

    def __init__(self):
        self.tasks = []
        self.tasks.append(Pomodoro())
        self.tasks.append(Pomodoro())
        self.tasks.append(Pomodoro())
        self.tasks.append(Pomodoro())
